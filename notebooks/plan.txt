HECHO (02_PREPROS reestructurado para RUL):
└── Resolución 10s nativa conservada (sin downsample a 1 min)
    Régimen 0 preservado para continuidad temporal
    → data/processed/preprocesado_completo.parquet (1.5M filas, base RUL)
    → data/processed/operacional.parquet (688K filas, rég. 1 y 2, 374 sesiones)

HECHO (03_EDA_degradacion - decisiones con evidencia):
└── RUL_MAX = 48 h (desviación de DV_pressure aparece 40-72h antes de cada falla)
    Ventana = 60 min (ACF estacional: memoria 1-2h, ciclo operacional ~9 min)
    Señales prioritarias: presiones (TP2, DV_pressure, Reservoirs, TP3, H1),
                          Oil_temperature, Motor_current + binarias de régimen, LPS
    Normalización por sesión para deriva térmica

HECHO (04_FE - feature engineering, reescrito y ejecutado sin errores):
└── Etiqueta RUL sobre preprocesado_completo (tiempo real a la próxima falla, censurado a 48h)
    Features sobre filas operacionales (rég. 1/2), todo por id_sesion (anti-leakage)
    Resolución 10s nativa; ventanas en nº de filas (60min=360, 30min=180, 15min=90)
    → Calendario cíclico (hora/día seno-coseno)
    → Dominio: delta_TP3_Reservoirs, TP2-TP3, temp_rate, ratio_carga, arranques, lps_count, z-scores
    → Tendencia/residuo por sesión (mediana móvil causal = ciclo; STL descartado por costo)
    → Ventana: media/std/min/max (15/30/60min) + pendiente vectorizada 60min
    → Lags de crudas + dominio (1/5/15 min)
    → Etiqueta binaria en_falla (umbral 24h)
    → data/processed/features_completas.parquet (517,825 filas × 168 cols, 162 features, 326 sesiones)

SIGUE (05_feature_importance):
└── RF rápido sobre features_completas → importancia, resolver TP3↔Reservoirs,
    decidir nº de lags, guardar metadata/features_seleccionadas.json
